// Amalgamated EEZ Studio LVGL UI export.
//
// The generated sources live in eez-ui-export/ (repo root, kept OUT of the
// auto-compiled src/ tree so the non-LVGL build envs never see them). The
// openevse_p4 env adds `-I eez-ui-export`; this single gated translation unit
// pulls them into the build only when the EEZ UI is enabled. Regenerate by
// re-exporting from EEZ Studio over the files in eez-ui-export/.
#if defined(ENABLE_SCREEN_LVGL) && defined(ENABLE_EEZ_UI)
#include "styles.c"
#include "images.c"
#include "ui_image_open_evse_logo.c"
#include "screens.c"
#include "ui.c"
#endif
