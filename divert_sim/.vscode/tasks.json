{
  "tasks": [
    {
      "type": "shell",
      "label": "build",
      "command": "/usr/bin/make",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": [
        "$gcc"
      ]
    }
  ],
  "version": "2.0.0"
}